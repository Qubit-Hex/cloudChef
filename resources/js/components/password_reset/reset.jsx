/**
 *
 *  @file: reset.jsx
 *
 *  @purpose : inorder to render the reset password screen
 *
 */




import React from "react";
import ReactDOM from "react-dom";
import FetchServiceProvider from "../../lib/fetchServiceProvider";


/**
 *
 *  @component: Reset
 *
 *
 *  @purpose : inorder to render the reset password screen
 *
 */

export const ResetPage = (props) => {

    // preform the password reset request via the api
    const request = () => {
        // preform the password reset request via the api
    }

    // request inorder to verfiy the token of the user
    // inorder to preform the password reset request via the api.
    const verifyToken = () => {

    }



    return (
        <div>

        </div>
    );
}
